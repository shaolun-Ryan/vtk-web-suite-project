#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_vtk.js@13.4.2@vtk.js/Utilities/XMLConverter/xml2json-cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../_vtk.js@13.4.2@vtk.js/Utilities/XMLConverter/xml2json-cli.js" "$@"
  ret=$?
fi
exit $ret
